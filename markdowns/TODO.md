# TODO — Format PQRST_mapping/refs.bib

## Steps
- [x] 1. Normalize all entry types to lowercase (`@article`, `@inproceedings`)
- [x] 2. Reorder fields to a single consistent layout across all entries
- [x] 3. Remove `""` double-quote artifacts, trailing whitespace, and extra blank lines
- [x] 4. Align `=` signs neatly within each entry; separate entries with a single blank line
- [x] 5. Preserve all bibliographic data (keys, authors, titles, DOIs, URLs) unchanged
- [x] 6. Validate that the resulting .bib file parses correctly

